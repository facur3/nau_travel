{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "postinstall": "git lfs install && git lfs fetch --all && git lfs checkout"
  }
}
